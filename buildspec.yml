version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - mkdir dist
      - npm run build
artifacts:
  files:
    - "dist/*"
  discard-paths: no
  base-directory: build
